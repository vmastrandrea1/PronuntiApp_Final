<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sfondo rettangolo personalizzato -->
    <item>
        <shape>
            <corners android:radius="20dp" />
            <solid android:color="#FFD700" />
        </shape>
    </item>

    <!-- Immagine sovrapposta con margine -->
    <item
        android:drawable="@drawable/primo_posto"
        android:gravity="center"
        android:width="50dp"
        android:height="50dp"
        android:top="75dp" />

</layer-list>
